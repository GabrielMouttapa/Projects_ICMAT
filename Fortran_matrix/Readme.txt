

1) Create a program with a namelist (it's like parameters) with
K= 9, size of the matrix
target = "matrix.txt"

2) Bash to remplace parameters (sed command)

3) execut the code matrix.f90 but with write instead of print

4) Paralelise the code with bash
